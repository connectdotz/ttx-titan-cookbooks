#!/bin/bash
#
# Copyright 2013 ConnectDotz, LLC. All Rights Reserved.
#

# Set Hadoop-specific environment variables here.

# The only required environment variable is JAVA_HOME.  All others are
# optional.  When running a distributed configuration it is best to
# set JAVA_HOME in this file, so that it is correctly defined on
# remote nodes.

export HADOOP_PREFIX="<%=  node[:ttx_hbase][:hadoop][:home] %>"
export HADOOP_CONF_DIR="<%=  node[:ttx_hbase][:hadoop][:conf] %>"
export HADOOP_PID_DIR=<%= node[:ttx_hbase][:hadoop][:pid_dir] %>
export HADOOP_IDENT_STRING=<%= node[:ttx_hbase][:hadoop][:user] %>


export HADOOP_LOG_DIR="<%= node[:ttx_hbase][:hadoop][:log_dir] %>"

#export HADOOP_MASTER=
#export HADOOP_IDENT_STRING=hadoop
export HADOOP_NICENESS=0

export JAVA_HOME="<%= node[:ttx_hbase][:java][:home] %>"
export HADOOP_OPTS="<%= node[:ttx_hbase][:hadoop][:hadoop_opts] %>" 

